package exec

/**
 * Created by pedrofurla on 01/04/14.
 */
object CSV {

/*  import shapeless._

  import Reports._
  import Chronograph._

  val sampleR = ReportLine(1,1,Micros)
  def reportLineH = Generic[ReportLine].to _

  val sampleH = reportLineH(sampleR)

  val sample = "A" :: 1 :: 2L :: HNil

  trait DefaultPoly1 extends Poly1 {
    implicit def default[T]=at[T](identity)
  }

  object stringify extends Poly1 with DefaultPoly1 {
    implicit val caseInt = at[Int](_.toString)
    implicit val caseChronon = at[Chronon](_.toString)
    implicit val caseTime = at[TimeUnit](_.name)
  }


  sampleH.map(stringify):Unit
  //HNil map stringfy
  //choose(sampleH)*/
}
